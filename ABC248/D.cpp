#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)

int main (void){
    ifstream in("./../input.txt");
    cin.rdbuf(in.rdbuf());

    int N, Q;
    cin >> N;

    int a;
    rep(i, N){
        cin >> a;
        A.at(0).at()

    }
    cin >> Q;
    int L, R, X;
    rep(i, Q){
        cin >> L >> R >> X;

    }

    return 0;
}